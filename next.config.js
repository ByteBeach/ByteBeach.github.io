/** @type {import('next').NextConfig} */
const nextConfig = 
{
    output: 'export',
    basePath: "/ByteBeach.github.io/out",
    images: {
        unoptimized: true,
      },
}

module.exports = nextConfig
