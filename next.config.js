/** @type {import('next').NextConfig} */
const nextConfig = 
{
    output: 'export',
    basePath: "/Bytebeach.github.io",
    images: {
        unoptimized: true,
      },
}

module.exports = nextConfig
