/** @type {import('next').NextConfig} */
const nextConfig = 
{
    output: 'export',
    basePath: "/byebeach.github.io",
    images: {
        unoptimized: true,
      },
}

module.exports = nextConfig
